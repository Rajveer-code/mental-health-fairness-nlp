# requirements.txt
# Pinned dependency versions matching the CPFE study environment.
# Install: pip install -r requirements.txt
#
# Hardware: experiments run on NVIDIA GPU with CUDA 11.8.
# CPU-only mode is supported but training will be significantly slower.

# ── Deep learning ─────────────────────────────────────────────────────────────
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# ── NLP / HuggingFace stack ───────────────────────────────────────────────────
transformers==4.36.0
datasets==2.16.0
tokenizers==0.15.0
huggingface-hub==0.20.3

# ── Interpretability ──────────────────────────────────────────────────────────
captum==0.7.0
shap==0.44.0

# ── Statistical / ML ─────────────────────────────────────────────────────────
scikit-learn==1.3.2
scipy==1.11.4
numpy==1.26.2
pandas==2.1.4
imbalanced-learn==0.11.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib==3.8.2
seaborn==0.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.66.1
pyyaml==6.0.1
rapidfuzz==3.5.2

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter==1.0.0
ipykernel==6.28.0

# ── Optional: GPT-4 evaluation (gpt_eval.py — Section 4, not yet implemented) ─
# openai>=1.0.0
